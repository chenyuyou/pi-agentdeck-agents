---
description: Delegate reconnaissance to the explorer subagent
argument-hint: "<area, question, or files to scope>"
---

Use the Agent tool with `subagent_type: "explorer"` and `run_in_background: false` to answer this,
then report its findings verbatim and add nothing of your own:

$ARGUMENTS

If the explorer reports that the request is really planning or implementation work, say so and stop.
